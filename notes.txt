Alpino and BaseX need to be restarted if the Vagrant Box is reloaded.
Puppet would also need to be rechecked in that case.
The link to GrETEL-upload from gretel doesn't work yet (it points to /gretel/gretel-upload instead of just /gretel-upload).
